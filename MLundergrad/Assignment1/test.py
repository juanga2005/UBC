#Script to test the power of Python

#line='and now for something completely different'
#
#for word in line.split():
#	es=sum([1 for x in word if x=='e'])
#	print "'%s' e-count=%d" % (word,es)
#

A=array([1,2.0,4])

e1=array([1,0,0]);e2=array([0,1,0])

M=outer(A,A)
